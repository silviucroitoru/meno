type Responses = Record<string, unknown>;

export function getRecommendations(responses: Responses): string[] {
  // Placeholder — return generic recommendations.
  // Replace with actual rule-based logic once questions are finalized.
  return [
    "Konsultacija sa endokrinologom",
    "Laboratorijske analize (glukoza, insulin, HbA1c, HOMA-IR)",
    "Prilagođen plan ishrane",
    "Program fizičke aktivnosti",
  ];
}
